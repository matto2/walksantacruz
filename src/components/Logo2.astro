---
import FootprintsIcon from '../assets/SVG/footprints-ICON.svg';

interface Props {
	class?: string;
	iconColor?: string;
	textColor?: string;
}

const {
	class: className,
	iconColor = 'currentColor',
	textColor = 'text-primary'
} = Astro.props;
---

<div class={`flex items-center gap-2 ${className || ''}`}>
	<FootprintsIcon width={32} height={32} class="flex-shrink-0" fill={iconColor} />
	<h1 class={`text-xl ${textColor}`} style="color: #333333;">Walk Santa Cruz</h1>
</div>
